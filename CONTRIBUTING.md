# 前端开发规范

本规范适用于 `frontend` 下的所有 Vue、TypeScript、CSS 和配置文件。自动化配置是格式与静态检查的最终依据；文档用于约定无法完全自动检查的设计选择。

## 1. 基础约定

- 使用 Node.js LTS、`pnpm` 和 `package.json` 声明的 pnpm 版本。
- 文件编码统一为 UTF-8，换行符统一为 LF，缩进统一为 2 个空格。
- JavaScript/TypeScript 不写分号，使用单引号，单行建议不超过 120 个字符。
- 禁止提交 `dist`、`node_modules`、`*.tsbuildinfo`、日志及编辑器临时文件。
- 提交前必须运行 `pnpm check`；合并前还应运行 `pnpm build`。

## 2. 目录职责

| 目录              | 职责                                               |
| ----------------- | -------------------------------------------------- |
| `src/api`         | 后端 IPC 客户端及接口封装                          |
| `src/components`  | 可复用组件，按 `ui`、`layout`、`modals` 等领域归类 |
| `src/composables` | 可复用的组合式状态和副作用逻辑                     |
| `src/config`      | 静态配置与默认值                                   |
| `src/constants`   | 无副作用的业务常量                                 |
| `src/i18n`        | 国际化初始化与语言资源                             |
| `src/plugin-sdk`  | 面向插件的稳定公共接口                             |
| `src/router`      | 路由声明和全局路由守卫                             |
| `src/styles`      | 全局样式及与组件目录镜像的样式文件                 |
| `src/types`       | 跨模块共享的 TypeScript 类型                       |
| `src/utils`       | 无状态、通用的纯工具函数                           |
| `src/views`       | 与路由对应的页面组件                               |

业务只在一个页面使用时，优先放在该页面附近；被两个及以上模块复用后再提升到公共目录。不要建立含义模糊的 `helpers`、`common2` 或 `misc` 目录。

## 3. 文件和标识符命名

- Vue 组件、页面：`PascalCase.vue`，例如 `TaskQueuePanel.vue`。
- Composable：`useXxx.ts`，导出函数同名，例如 `useTaskQueue`。
- 普通 TypeScript 文件和目录：使用小写单词；延续既有目录时保持同一风格。
- 类型、接口、枚举：`PascalCase`；变量和函数：`camelCase`；真正的模块级常量：`UPPER_SNAKE_CASE`。
- 布尔值使用 `is`、`has`、`can`、`should` 前缀；事件处理函数使用 `handleXxx`；组件事件使用 `kebab-case`。
- CSS 类名使用语义化 `kebab-case`，禁止用颜色、坐标或临时编号表达业务含义。

## 4. Vue 组件

- 新组件统一使用 Composition API 和 `<script setup lang="ts">`。
- 单文件组件块顺序为 `<template>`、`<script>`、`<style>`；项目当前优先使用 `src/styles` 下的独立样式文件。
- Props、Emits、Slots 必须声明类型；不要通过 `$attrs` 隐式传递核心业务参数。
- 模板只保留简单展示表达式。请求、复杂计算和副作用放入脚本或 composable。
- 计算值使用 `computed`，不要用 `watch` 维护可以推导出的重复状态。
- `watch`、事件监听器、定时器和后端订阅必须在组件卸载时清理。
- 页面组件负责流程编排，可复用展示和交互拆到 `components`；跨页面状态优先抽成 composable。
- 用户可见文本必须进入 `src/i18n/locales`，调试信息和协议原文除外。
- 交互元素必须支持键盘操作，并提供标签、焦点态及必要的 ARIA 属性。

推荐的脚本组织顺序：外部依赖、`@/` 内部依赖、类型导入、宏声明、注入与 composable、响应式状态、计算值、业务函数、监听和生命周期。

## 5. TypeScript 与后端通信

- 保持严格类型检查；禁止使用 `@ts-ignore` 隐藏错误。确需规避时使用带原因的 `@ts-expect-error`。
- 避免 `any`；不可信的后端、插件或存储数据先作为 `unknown`，经过校验或类型守卫后再使用。
- 仅用于类型的依赖使用 `import type`。
- 公共函数、跨模块数据和 IPC 响应应显式声明返回类型；局部且含义清楚的变量可依赖类型推断。
- 前端只通过 `src/api/client.ts` 调用后端，不在组件内直接调用 PyTauri 全局对象。
- 新后端命令必须同步更新 `src/types/api.ts` 和 `BACKEND_API.md`，并统一处理失败、超时与空数据。
- 插件 SDK 是公共兼容边界。修改导出类型、命令、事件或挂载点时必须考虑向后兼容并记录变更。

## 6. 导入、状态和错误处理

- 导入由 ESLint 自动排序：第三方依赖在前，`@/` 内部模块在后，类型导入由规则统一处理。
- 使用 `@/` 引用 `src` 内跨目录模块；只在同目录或紧邻模块中使用相对路径。
- 不在模块顶层执行网络请求、DOM 修改或不可撤销副作用。
- 异步操作必须体现加载、成功、空数据和失败状态；禁止静默吞掉会影响用户的错误。
- 仅日志基础设施可直接使用 `console`；业务提示使用项目统一消息或错误弹窗。
- 清理失败可忽略时，要用简短注释说明原因。

## 7. CSS 与主题

- 优先复用 `src/styles` 中的设计变量、间距、圆角和动画，避免散落魔法数。
- 颜色和主题相关值使用 CSS 自定义属性，不在组件中硬编码浅色或深色主题值。
- 选择器保持低特异性，原则上不使用 `!important`；覆盖第三方样式时限制在明确的组件容器内。
- 动画必须兼容 `prefers-reduced-motion`，并避免对布局属性执行高频动画。
- 页面样式放入 `styles/views`，组件样式放入与 `components` 镜像的目录；文件名与对应 Vue 文件一致。
- 修改界面时至少检查启动器最小窗口尺寸、长文本、中文与英文界面。

## 8. 注释、文档与提交

- 注释解释“为什么”，不复述代码；公共 SDK、复杂算法和不直观的兼容逻辑使用 TSDoc。
- TODO 必须说明待办事项和上下文，能关联问题编号时一并标注。
- 一个提交只处理一个主题，建议使用 `feat:`、`fix:`、`refactor:`、`style:`、`docs:`、`test:` 或 `chore:` 前缀。
- 不把纯格式化与业务逻辑修改混在同一提交中。

## 9. 提交前清单

- `pnpm check` 通过。
- `pnpm build` 通过。
- 新增用户文本已补齐中英文翻译。
- 新增异步流程具备加载、失败和取消/清理逻辑。
- 修改后端命令或插件公共接口时，类型与文档已同步。
- UI 修改已检查键盘操作、焦点态、最小窗口尺寸和减少动画设置。
