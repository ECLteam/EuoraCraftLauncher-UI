# 前端样式重构基线

## 目标

在不改变 IPC 契约和业务流程的前提下，提高导航可辨识度、任务操作效率、信息可读性和不同窗口尺寸下的稳定性。Showcase 继续作为视觉验收入口。

## 参考项目审计

### HMCL

- 侧边栏按“账户、版本、设置”等任务域分组，当前位置与信息层级清晰。
- 列表广泛使用“主标题 + 次要说明”的两行结构，路径、版本和状态不必挤在同一行。
- 启动操作固定在页面右下方，并同时展示当前版本；用户始终知道下一步会启动什么。
- 内容页采用约 20px 外边距，提示、错误、警告使用稳定的语义色和容器。

### PCL

- 顶层导航保持精简，进入任务后再显示上下文侧栏，减少一次展示的选项数量。
- 启动按钮尺寸和对比度显著高于辅助操作，版本名称直接附着于启动区域。
- 版本安装采用渐进式流程：先选游戏版本，再选择加载器和附加组件，底部保留明确的最终操作。
- 页面通常使用 20–25px 内容边距，卡片标题、内容和操作位置高度一致。

### PCL-CE

- 延续 PCL 的任务结构，并允许窗口自由缩放，最小宽度约 810px。
- 使用统一图标集，图标只辅助文字，不取代关键操作名称。
- 新闻和资源类内容采用自适应列宽与虚拟化布局，兼顾宽屏利用率和窄屏可读性。
- 加载、空状态、错误和后台任务入口均有固定位置，页面状态切换更容易理解。

## 当前界面问题

- 主侧栏默认折叠时只剩图标，首次使用者难以建立功能与图标之间的映射。
- 活动指示器会跟随悬停移动，容易把“当前页面”和“即将点击”混淆。
- 43px 标题栏、38px 导航行和 11–12px 辅助文字整体偏紧凑。
- 设置页在宽屏中仍限制为 540px 且靠左，形成大面积无意义留白。
- 全局页面边距、上下文侧栏宽度和活动态表现缺少统一基线。

## 第一阶段样式基线

- 窗口最小宽度：820px。
- 标题栏高度：48px；窗口按钮：32px。
- 主侧栏：默认展开，展开宽度 196px，折叠宽度 56px。
- 一级导航行：42px；二级导航行：34px。
- 页面外边距：20px，窄窗口下降至 14px。
- 设置表单阅读宽度：720px，居中展示。
- 活动态使用固定浅色背景、主色文字和左侧 3px 指示条；悬停只改变背景，不移动当前位置。
- 卡片和浮层使用轻量边框与一级阴影，保持浅色、深色主题的一致层级。

## 页面改造顺序

1. 全局框架：标题栏、主侧栏、页面边距、上下文导航。
2. 游戏首页：强化启动主操作，整理账户与公告信息层级。
3. 账户管理：两行账户项、明确当前账户与登录类型。
4. 版本安装：渐进式选择、兼容性提示、固定安装操作。
5. 版本管理与插件：统一列表密度、筛选栏和批量/危险操作。
6. 设置：统一表单行、控件宽度、帮助说明和错误反馈。

## 验收

- 1600×900、1280×720、950×600 三档窗口均无水平溢出。
- 仅查看导航文字即可完成主要任务，不依赖图标猜测。
- 当前页面、悬停项、禁用项和危险操作能够一眼区分。
- 生产模式和 Showcase 模式保持相同布局；Showcase 不产生 IPC 请求。
